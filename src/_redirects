# Cloudflare Pages Redirects
# Note: Subdomain routing is handled by _middleware.js Functions
# This file only provides fallbacks for static asset paths

# Static assets - no redirect needed (served directly)
/assets/* /assets/:splat 200
/output.css /output.css 200

# Subdirectory access (when middleware rewrites to these paths)
/drivers/* /drivers/:splat 200
/vendor/* /vendor/:splat 200
/user/* /user/:splat 200
/promo/* /promo/:splat 200
